# Moldova Labor Code RAG (RAG-система по Трудовому кодексу Молдовы)

Модульная Retrieval-Augmented Generation (RAG) система по законодательству Республики Молдова (Трудовой кодекс), реализованная на чистом Python без тяжелых фреймворков (LangChain / LlamaIndex).

## Стек технологий и архитектура

- **Язык**: Python 3.10+
- **Векторная БД**: ChromaDB (локальное хранилище)
- **Эмбеддинги**: Sentence-Transformers (легкие модели для CPU: `rubert-tiny2` / `bge`)
- **Reranker**: FlashRank / Cross-Encoder (локально на CPU)
- **LLM**: Google Gemini Flash через официальный API
- **Архитектура**: Чистый модульный пайплайн с разделением ответственности:
  - `src/grabber/` — Сбор и дедупликация нормативных документов
  - `src/preprocessing/` — Парсинг HTML, очистка структуры и чанкинг по статьям
  - `src/embeddings/` — Векторизация и управление векторным индексом
  - `src/retrieval/` — Семантический поиск и фильтрация
  - `src/reranking/` — Переранжирование кандидатов
  - `src/generation/` — Формирование контекста и вызов LLM с защитой от галлюцинаций
  - `src/evaluation/` — Набор тестов и метрики качества (Hit Rate, MRR)

---

## Структура репозитория

```text
├── configs/
│   ├── sources.json             # Конфигурация источников документов
│   ├── embeddings.json          # Параметры моделей эмбеддингов и ChromaDB
│   └── retrieval.json           # Параметры поиска, фильтрации и реранкинга
├── data/
│   ├── raw/                     # Исходные HTML-документы, метаданные и манифест
│   │   ├── codul_muncii.html    # Трудовой кодекс РМ (Закон № 154/2003)
│   │   ├── manifest.json        # Хеши и статус для дедупликации
│   │   └── metadata.json        # Метаданные загруженного акта
│   └── processed/               # Сгенерированные наборы чанков (JSON)
│       ├── chunks_by_article.json
│       ├── chunks_fixed_size.json
│       ├── chunks_fixed_overlap.json
│       └── chunks_by_paragraph.json
├── experiments/                 # Скрипты сравнения гиперпараметров и графики
├── src/
│   ├── grabber/                 # Модуль сбора документов с legis.md
│   │   └── legis_grabber.py
│   ├── preprocessing/           # Модуль очистки, парсинга и чанкинга
│   │   ├── cleaner.py           # Очистка HTML через BeautifulSoup
│   │   ├── parser.py            # Выделение разделов, глав и статей через RegEx
│   │   ├── chunker.py           # Класс TextChunker (4 стратегии)
│   │   └── pipeline.py          # Главный конвейер предобработки
│   ├── embeddings/              # Векторизация и работа с векторным индексом
│   │   ├── embedder.py          # Класс EmbeddingModel (SentenceTransformer на CPU)
│   │   └── indexer.py           # Класс VectorIndexer (ChromaDB хранилище и батчинг)
│   ├── retrieval/               # Поиск документов и фильтрация контекста
│   │   ├── retriever.py         # Класс VectorRetriever (поиск Top-K в ChromaDB)
│   │   ├── filters.py           # Класс ContextFilter (порог, длина, дедупликация)
│   │   └── search_pipeline.py   # Сквозной пайплайн поиска и CLI демонстрация
│   ├── reranking/               # Реранкинг результатов
│   │   └── reranker.py          # Класс DocumentReranker (FlashRank Cross-Encoder на CPU)
│   ├── generation/              # Генерация ответа
│   └── evaluation/              # Оценка метрик
├── PROJECT_SPEC.md              # Архитектурные требования и спецификация
├── requirements.txt             # Зависимости проекта
└── README.md
```

---

## Этап 1: Сбор данных (Grabber)

В рамках первого этапа реализован отказоустойчивый модуль автоматического сбора данных с государственного портала [legis.md](https://www.legis.md/):
- **Управление языковой сессией**: Автоматическая инициализация сессии через `https://www.legis.md/languages/index/ru` с сохранением cookie (`ci_session`) для получения актуального текста кодекса на русском языке.
- **Сборка полного документа**: Корректное объединение базовой разметки и динамически подгружаемого тела акта (`showdetails/{doc_id}`).
- **Стабилизация хеша**: Очистка динамических токенов Cloudflare (`__CF$cv$params`), гарантирующая детерминированный SHA-256 хеш.
- **Дедупликация**: Проверка SHA-256 по `data/raw/manifest.json`. Если нормативный акт не изменялся, повторное скачивание пропускается.
- **Метаданные**: Фиксация даты загрузки, URL и контрольной суммы в `metadata.json`.

---

## Этап 2: Предобработка и чанкинг данных (Preprocessing & Chunking)

В рамках второго этапа реализован модульный конвейер предварительной обработки и многостратегического чанкинга нормативного текста:

### 1. Очистка HTML (`src/preprocessing/cleaner.py`)
- **Извлечение контента**: Поиск основного контейнера документа (`div#contentdoc.doc`) с помощью `BeautifulSoup`.
- **Удаление служебных блоков**: Декомпозиция тегов `<script>`, `<style>`, `<button>`, `<form>`, `<nav>`, `<header>`, `<footer>`, `<iframe>` и элементов интерфейса (попапы, модалки, кнопки печати, логотипы).
- **Сохранение надстрочных индексов (`<sup>`)**: Преобразование тегов `<sup>` в формат `^{...}` (например, `Статья 7<sup>1</sup>` $\rightarrow$ `Статья 7^1`, пункты вида `b^1`), что исключает коллизии и склеивание статей с обычными номерами (например, исключает смешение статей `7^1` и `71`).
- **Нормализация символов**: Замена неразрывных пробелов `\xa0` и `\u200b` на стандартные пробелы, схлопывание множественных пробелов и нормализация избыточных переводов строк (`\n{3,}` $\rightarrow$ `\n\n`).

### 2. Синтаксический анализ структуры (`src/preprocessing/parser.py`)
- **Извлечение иерархии через RegEx**: Выделение разделов (`^РАЗДЕЛ\s+[IVXLCDM\d]+`), глав (`^Глава\s+[IVXLCDM\d]+`) и статей (`^Статья\s+\d+...`).
- **Склеивание многострочных заголовков**: Обработка переносов строк в заголовках статей и разделов, возникших при конвертации из Word в HTML.
- **Полнота извлечения**: Извлечены **14 разделов**, **66 глав** и **430 статей** кодекса без коллизий и потерь.
- **Атрибуты статьи**: Каждая статья содержит `number` (номер), `title` (заголовок), `text` (полный текст статьи), `body` (тело статьи без заголовка), `paragraphs` (список отдельных абзацев/пунктов), `section` (раздел) и `chapter` (глава).

### 3. Стратегии чанкинга (`src/preprocessing/chunker.py`)

Класс `TextChunker` реализует 4 стратегии разбиения текста:

| Стратегия | Параметры | Описание |
|---|---|---|
| **by_article** | — | Статья целиком является одним чанком. Основной рабочий набор для юридического RAG. |
| **fixed_size** | `chunk_size=500` | Нарезка текста статьи фиксированными блоками по 500 символов. |
| **fixed_overlap** | `chunk_size=500`, `overlap=100` | Нарезка блоками по 500 символов с нахлестом 100 символов (шаг 400 символов). |
| **by_paragraph** | — | Разбиение статьи по отдельным абзацам и пунктам (`paragraphs`). |

### 4. Структура метаданных чанка

Каждый чанк содержит уникальный детерминированный `chunk_id` и структурированные метаданные:

```json
{
  "chunk_id": "codul_muncii_by_article_art_1",
  "text": "Статья 1. Основные понятия\n\nВ настоящем кодексе следующие основные понятия используются в значении:\n...",
  "source": "codul_muncii",
  "url": "https://www.legis.md/cautare/getResults?doc_id=155882&lang=ru",
  "title": "Основные понятия",
  "section": "РАЗДЕЛ I. ОБЩИЕ ПОЛОЖЕНИЯ",
  "document_id": "codul_muncii",
  "updated_at": "2026-09-14T20:22:22.422099+00:00",
  "chunk_strategy": "by_article",
  "metadata": {
    "source": "codul_muncii",
    "url": "https://www.legis.md/cautare/getResults?doc_id=155882&lang=ru",
    "title": "Основные понятия",
    "section": "РАЗДЕЛ I. ОБЩИЕ ПОЛОЖЕНИЯ",
    "chapter": "Глава I. ВВОДНЫЕ ПОЛОЖЕНИЯ",
    "document_id": "codul_muncii",
    "updated_at": "2026-09-14T20:22:22.422099+00:00",
    "chunk_strategy": "by_article",
    "article_number": "1",
    "chunk_index": 0
  }
}
```

### 5. Сводная статистика результатов чанкинга

Конвейер запускается командой:
```bash
python -m src.preprocessing.pipeline
```

Полученная статистика по результатам обработки:

| Стратегия чанкинга | Чанков | Средний размер (симв.) | Мин..Макс (симв.) | Файл результата |
|---|---|---|---|---|
| **by_article** *(основной набор)* | **430** | 1005.5 | 22 .. 12395 | `data/processed/chunks_by_article.json` |
| **fixed_size** | **1076** | 401.7 | 2 .. 500 | `data/processed/chunks_fixed_size.json` |
| **fixed_overlap** | **1198** | 424.8 | 22 .. 500 | `data/processed/chunks_fixed_overlap.json` |
| **by_paragraph** | **2058** | 194.1 | 3 .. 1132 | `data/processed/chunks_by_paragraph.json` |

---

## Этап 3: Векторизация и Vector Database (Embeddings & Vector Indexing)

В рамках третьего этапа реализована подсистема векторизации и локального хранения векторных индексов нормативных документов на базе `Sentence-Transformers` и `ChromaDB`:

### 1. Обоснование выбора моделей эмбеддингов

В конфигурационном файле `configs/embeddings.json` зарегистрированы две взаимодополняющие модели, оптимизированные для быстрого и точного инференса на CPU:

| Модель / Алиас | HuggingFace Identifier | Размерность | Язык | Назначение и обоснование |
|---|---|---|---|---|
| **rubert-tiny2** *(по умолчанию)* | `cointegrated/rubert-tiny2` | **312** | ru | Компактная модель (~29M параметров, ~118 МБ). Обучена на корпусах русскоязычных текстов, превосходно понимает юридическую терминологию и синтаксис ТК РМ, обеспечивая максимальную скорость векторизации на CPU. |
| **multilingual-e5-small** | `intfloat/multilingual-e5-small` | **384** | multilingual (100+ языков) | Высокоточная мультиязычная модель (~118M параметров, ~470 МБ) с поддержкой асимметричного поиска. Требует префикс `query: ` для запросов, что реализовано автоматически в методе `encode_query()`. Идеальна для сравнительных тестов и мультиязычных запросов (RU/RO/EN). |

- **Инференс на CPU**: Принудительная инициализация с `device="cpu"` гарантирует полную переносимость и работу на любой рабочей станции без зависимости от дискретных видеокарт CUDA.
- **Нормализация векторов**: Все методы кодирования документов (`encode_documents`) и поисковых запросов (`encode_query`) используют `normalize_embeddings=True` ($L_2$-нормализация), что позволяет рассчитывать косинусное сходство через скалярное произведение, оптимизируя вычисления в HNSW.

### 2. Обоснование выбора ChromaDB

В качестве векторного хранилища выбрана **ChromaDB**:
- **Встраиваемая (Embedded) архитектура**: Работает напрямую в процессе Python через `chromadb.PersistentClient(path="chroma_db")` без необходимости запуска отдельных контейнеров Docker или сетевых сервисов (в отличие от Qdrant, Milvus или Weaviate).
- **Метрика расстояния Cosine**: Коллекции инициализируются с параметром `metadata={"hnsw:space": "cosine"}`, что строго соответствует $L_2$-нормализованным эмбеддингам.
- **Идемпотентность (`upsert`)**: Загрузка векторов выполняется через метод `upsert()`, гарантируя безопасный повторный запуск без риска дублирования чанков.
- **Локальная персистентность**: Все индексы и метаданные сохраняются в директорию `chroma_db/` (добавлена в `.gitignore`).

### 3. Конфигурация (`configs/embeddings.json`)

```json
{
  "default_model": "rubert-tiny2",
  "models": {
    "rubert-tiny2": {
      "name": "cointegrated/rubert-tiny2",
      "dimensions": 312,
      "language": "ru"
    },
    "multilingual-e5-small": {
      "name": "intfloat/multilingual-e5-small",
      "dimensions": 384,
      "language": "multilingual"
    }
  },
  "batch_size": 32,
  "persist_directory": "chroma_db"
}
```

### 4. Запуск индексации и бенчмарк

Индексация основного набора чанков (`chunks_by_article.json`) запускается командой:
```bash
python -m src.embeddings.indexer
```

Также поддерживается индексация альтернативных моделей и наборов:
```bash
# Индексация с мультиязычной моделью multilingual-e5-small
python -m src.embeddings.indexer --model multilingual-e5-small

# Индексация чанков с перекрытием (fixed_overlap)
python -m src.embeddings.indexer --file data/processed/chunks_fixed_overlap.json --model rubert-tiny2
```

#### Результаты построения индекса (CPU, batch_size=32):
- **Объем данных**: 430 статей Трудового кодекса (`chunks_by_article.json`)
- **Модель**: `cointegrated/rubert-tiny2` (312 dim)
- **Время построения индекса**: **~19.0 секунд**
- **Скорость векторизации**: **~22.6 чанков/сек**
- **Коллекция ChromaDB**: `chunks_by_article_rubert-tiny2` (430 векторов)

---

## Этап 4: Поиск, фильтрация и реранкинг (Retriever, Filters & Reranker)

В рамках четвертого этапа реализован двухступенчатый поисковый конвейер (*two-stage retrieval pipeline*), связывающий быстрый векторный отбор кандидатов, отсечение контекстного шума и высокоточное переранжирование на локальном CPU.

### 1. Архитектура поискового конвейера

```text
[Поисковый запрос]
       │
       ▼
[VectorRetriever]  ──>  Векторизация (rubert-tiny2) + поиск в ChromaDB (Top-20)
       │
       ▼
[ContextFilter]    ──>  1. filter_by_min_length (>= 30 символов)
                   ──>  2. filter_by_threshold (similarity_score >= 0.35)
                   ──>  3. deduplicate_by_article (наиболее релевантный чанк на статью)
       │
       ▼
[DocumentReranker] ──>  Cross-Encoder (FlashRank: ms-marco-MiniLM-L-12-v2 на CPU)
       │
       ▼
[Финальный Top-5]   ──>  Отранжированный релевантный контекст для генерации ответа LLM
```

### 2. Векторный поиск Top-K (`src/retrieval/retriever.py`)

Класс `VectorRetriever` отвечает за быстрый первичный отбор кандидатов:
- **Подключение к ChromaDB**: Работает с существующей локальной коллекцией векторов (`chunks_by_article_rubert-tiny2`) через `chromadb.PersistentClient`.
- **Векторизация запроса**: Использует класс `EmbeddingModel` (`cointegrated/rubert-tiny2`) с нормализацией векторов ($L_2$-norm).
- **Расчет метрики сходства**: ChromaDB возвращает косинусное расстояние $d = 1 - \cos(\theta)$. Метод `retrieve()` рассчитывает показатель семантического сходства по формуле:
  ```text
  similarity_score = 1.0 - distance
  ```
  где `1.0` соответствует полному совпадению направлений векторов, а `0.0` — ортогональности.
- **Выходной формат**: Список словарей с ключами `id`, `text`, `metadata`, `distance`, `similarity_score`, отсортированный по убыванию сходства.

### 3. Фильтрация контекста (`src/retrieval/filters.py`)

Класс `ContextFilter` очищает пул кандидатов от нерелевантных, коротких и избыточных фрагментов:
1. **`filter_by_min_length(min_chars=30)`**: Удаляет артефакты разметки, пустые строки и обрывочные фрагменты короче 30 символов.
2. **`filter_by_threshold(threshold=0.45)`**: Отсекает семантический шум — документы, чей `similarity_score` ниже заданного порога отсечения (значение 0.45 надежно отсекает нерелевантные общие запросы, сохраняя высокую полноту для профильных юридических вопросов).
3. **`deduplicate_by_article()`**: Устраняет дубликаты фрагментов одной и той же статьи кодекса (особенно актуально при стратегиях `fixed_size`, `fixed_overlap`, `by_paragraph`), оставляя наиболее релевантный чанк с максимальным `similarity_score`.
4. **`apply_all(config)`**: Последовательно применяет всю цепочку фильтров.

### 4. Переранжирование Cross-Encoder (`src/reranking/reranker.py`)

#### Принцип работы и обоснование выбора FlashRank:
- **Ограничение Bi-Encoder (эмбеддингов)**: Модели эмбеддингов кодируют запрос и документ *независимо* в фиксированные векторы (312 dim). При этом теряется тонкое контекстное взаимодействие между терминами запроса и сложными юридическими формулировками закона.
- **Преимущество Cross-Encoder**: В Cross-Encoder запрос и документ подаются в трансформер *совместно*. Механизм Self-Attention на всех слоях напрямую оценивает взаимную релевантность терминов запроса и фрагментов статьи, что исключает ложные срабатывания и поднимает целевые статьи на первые позиции.
- **Инференс на CPU (FlashRank)**: Используется библиотека `flashrank` с оптимизированной ONNX-моделью `ms-marco-MiniLM-L-12-v2`. Модель кэшируется локально в `models_cache/` и отрабатывает на CPU за десятки миллисекунд.
- **Отказоустойчивость (Fallback-режим)**: Если FlashRank недоступен или возникает непредвиденная ошибка, реранкер автоматически выполняет fallback, возвращая документы, ранжированные по базовому `similarity_score`.

### 5. Конфигурация параметров (`configs/retrieval.json`)

```json
{
  "retriever_top_k": 20,
  "reranker_top_n": 5,
  "similarity_threshold": 0.45,
  "min_chunk_chars": 30,
  "reranker_model": "ms-marco-MiniLM-L-12-v2"
}
```

### 6. Демонстрационный запуск поискового конвейера (`src/retrieval/search_pipeline.py`)

Запуск конвейера с базовым запросом:
```bash
python -m src.retrieval.search_pipeline
```

Запуск с произвольным юридическим запросом:
```bash
python -m src.retrieval.search_pipeline --query "Какой испытательный срок устанавливается при приеме на работу?"
```

#### Пример консольного вывода:

```text
================================================================================
ДЕМОНСТРАЦИЯ ПОИСКОВОГО КОНВЕЙЕРА (ЭТАП 4: RETRIEVER -> FILTERS -> RERANKER)
================================================================================
Поисковый запрос: "Какова продолжительность ежегодного оплачиваемого отпуска?"
Параметры: Top-K Retriever = 20 | Порог сходства >= 0.45 | Мин. символов = 30 | Top-N Reranker = 5
Воронка документов: Найдено в ChromaDB: 20 -> После фильтров: 20 -> В топе реранкера: 5

--------------------------------------------------------------------------------
1. ТОП-3 ДО РЕРАНКИНГА (Первичная векторная выдача VectorRetriever):
--------------------------------------------------------------------------------
  [1] Статья 113: «Продолжительность ежегодного оплачиваемого отпуска» (ID: codul_muncii_by_article_art_113)
      Балл: Similarity Score = 0.6522 (Vector Sim: 0.6522)
      Текст: Статья 113. Продолжительность ежегодного оплачиваемого отпуска  (1) Всем работникам ежегодно предоставляется оплачиваемый отпуск продолжительностью не менее 28 календарных дней без учета нерабочих праздничных дней.  (2) ...
  [2] Статья 118: «Ежегодное предоставление оплачиваемого отпуска. Исключительные случаи его перенесения» (ID: codul_muncii_by_article_art_118)
      Балл: Similarity Score = 0.6512 (Vector Sim: 0.6512)
      Текст: Статья 118. Ежегодное предоставление оплачиваемого отпуска. Исключительные случаи его перенесения  (1) Оплачиваемый отпуск предоставляется работнику ежегодно согласно графику, предусмотренному статьей 116. Работодатель о...
  [3] Статья 113^1: «Рабочий год, за который предоставляется ежегодный оплачиваемый отпуск» (ID: codul_muncii_by_article_art_113_1)
      Балл: Similarity Score = 0.6412 (Vector Sim: 0.6412)
      Текст: Статья 113^1. Рабочий год, за который предоставляется ежегодный оплачиваемый отпуск  (1) Рабочий год, за который предоставляется ежегодный оплачиваемый отпуск, составляет 12 месяцев, исчисляемых со дня приема на работу. ...

--------------------------------------------------------------------------------
2. ТОП-3 ПОСЛЕ РЕРАНКИНГА (Выдача Cross-Encoder FlashRank Reranker):
--------------------------------------------------------------------------------
  [1] Статья 113: «Продолжительность ежегодного оплачиваемого отпуска» (ID: codul_muncii_by_article_art_113)
      Балл: FlashRank Score = 0.9992 (Vector Sim: 0.6522)
      Текст: Статья 113. Продолжительность ежегодного оплачиваемого отпуска  (1) Всем работникам ежегодно предоставляется оплачиваемый отпуск продолжительностью не менее 28 календарных дней без учета нерабочих праздничных дней.  (2) ...
  [2] Статья 100: «Продолжительность ежедневной работы» (ID: codul_muncii_by_article_art_100)
      Балл: FlashRank Score = 0.9990 (Vector Sim: 0.5739)
      Текст: Статья 100. Продолжительность ежедневной работы  (1) Нормальная продолжительность ежедневной работы составляет 8 часов.  (2) Для работников в возрасте до шестнадцати лет продолжительность ежедневной работы не может превы...
  [3] Статья 118: «Ежегодное предоставление оплачиваемого отпуска. Исключительные случаи его перенесения» (ID: codul_muncii_by_article_art_118)
      Балл: FlashRank Score = 0.9990 (Vector Sim: 0.6512)
      Текст: Статья 118. Ежегодное предоставление оплачиваемого отпуска. Исключительные случаи его перенесения  (1) Оплачиваемый отпуск предоставляется работнику ежегодно согласно графику, предусмотренному статьей 116. Работодатель о...

--------------------------------------------------------------------------------
3. СРАВНИТЕЛЬНЫЙ АНАЛИЗ ВЛИЯНИЯ РЕРАНКИНГА:
--------------------------------------------------------------------------------
  • Лидер векторного поиска:    Статья 113 (Similarity = 0.6522)
  • Лидер после FlashRank:     Статья 113 (FlashRank Score = 0.9992)
  • Позиция целевой статьи 113 в первичном поиске: #1 -> поднялась на #1
================================================================================
```

---

## Установка и запуск

### 1. Клонирование репозитория
```bash
git clone https://github.com/obrazovan/moldova-labor-code-rag.git
cd moldova-labor-code-rag
```

### 2. Создание виртуального окружения
```bash
python -m venv venv

# Windows (PowerShell):
.\venv\Scripts\Activate.ps1

# Linux / macOS:
source venv/bin/activate
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 4. Запуск сбора данных (Этап 1: Grabber)
```bash
python -m src.grabber.legis_grabber
```

### 5. Запуск предобработки и чанкинга (Этап 2: Pipeline)
```bash
python -m src.preprocessing.pipeline
```

### 6. Запуск векторизации и построения индекса ChromaDB (Этап 3: Indexer)
```bash
python -m src.embeddings.indexer
```

### 7. Запуск поиска, фильтрации и реранкинга (Этап 4: Search Pipeline)
```bash
python -m src.retrieval.search_pipeline
```

